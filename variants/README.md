# Description

This directory contains variations of our standard images to support specific use cases.

# Structure

Each sub-folder will have a Dockerfile with additional packages being installed, and a Makefile with the tag being used.
